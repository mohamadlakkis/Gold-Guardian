<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gold Assistant</title>
    <style>
        /* General styling */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
            color: #333;
        }

        h1 {
            text-align: center;
            margin: 20px 0;
            color: #444;
        }

        .container {
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding: 20px;
            gap: 20px;
        }

        .form-container {
            width: 45%;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .form-container h2 {
            color: #555;
        }

        .form-container form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .form-container label {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .form-container input[type="text"],
        .form-container input[type="file"],
        .form-container button {
            padding: 10px;
            font-size: 1rem;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .form-container input[type="text"]:focus,
        .form-container input[type="file"]:focus,
        .form-container button:hover {
            border-color: #007bff;
            outline: none;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.3);
        }

        .form-container button {
            background: #007bff;
            color: #fff;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .form-container button:hover {
            background: #0056b3;
        }

        .results-container {
            width: 100%;
            margin-top: 20px;
        }

        .results-container h3 {
            color: #333;
        }

        .results-container img {
            max-width: 100%;
            height: auto;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .error-message {
            color: red;
            font-weight: bold;
            margin-top: 10px;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                align-items: center;
            }

            .form-container {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <h1>Gold Assistant</h1>
    <div class="container">
        <!-- Left Section: Text Query -->
        <div class="form-container">
            <h2>Text Query</h2>
            <form method="POST" action="/text-prompt">
                <label for="query">Ask a Question:</label>
                <input type="text" id="query" name="prompt" placeholder="e.g., How can I invest in Gold?" required>
                <button type="submit">Submit</button>
            </form>
        </div>

        <!-- Right Section: Image Query -->
        <div class="form-container">
            <h2>Image Query</h2>
            <form method="POST" action="/image_RAG" enctype="multipart/form-data">
                <label for="prompt">Describe your query for the image:</label>
                <input type="text" id="prompt" name="prompt" placeholder="e.g., Analyze trends in the chart" required>
                <label for="image">Upload an Image:</label>
                <input type="file" id="image" name="image" accept="image/*" required>
                <button type="submit">Submit Image Query</button>
            </form>
        </div>
    </div>

    <!-- Results Section -->
    <div class="results-container">
        {% if query %}
            <h3>Your Query:</h3>
            <p>{{ query }}</p>
            <h3>Answer:</h3>
            <p>{{ answer }}</p>
            <!-- <h2>Retrieved Documents:</h2>
            <ul>
                {% for doc in documents %}
                    <li>{{ doc }}</li>
                {% endfor %}
            </ul> -->
        {% endif %}

        {% if prompt %}
            <h3>Your Image Query:</h3>
            <p>{{ prompt }}</p>
            <h3>Uploaded Image:</h3>
            {% if image_URL %}
                <img src="{{ image_URL }}" alt="Uploaded Image">
            {% endif %}
            <h3>Answer:</h3>
            <p>{{ answer }}</p>
            <!-- <h2>Retrieved Documents:</h2>
            <ul>
                {% for doc in documents %}
                    <li>{{ doc }}</li>
                {% endfor %}
            </ul> -->
        {% endif %}

        {% if error %}
            <div class="error-message">{{ error }}</div>
        {% endif %}
    </div>
</body>
</html>
